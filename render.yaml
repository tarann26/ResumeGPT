services:
  - type: web
    name: resumegpt
    env: python
    buildCommand: pip install -e .[dev]
    startCommand: PYTHONPATH=/opt/render/project/src uvicorn resumegpt.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
