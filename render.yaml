services:
  - type: web
    name: resumegpt
    env: python
    buildCommand: pip install -e .[dev]
    startCommand: uvicorn resumegpt.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
